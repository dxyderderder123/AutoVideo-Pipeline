# Core dependencies
python-dotenv>=1.0.0
requests>=2.31.0
pyyaml>=6.0.1
openai>=1.0.0

# Audio processing
librosa>=0.10.1
soundfile>=0.12.1

# Image processing
pillow>=10.1.0

# Text processing
nltk>=3.8.1

# HTTP client for APIs
httpx>=0.25.2
aiohttp>=3.9.1

# Utilities
tqdm>=4.66.1
colorama>=0.4.6
rich>=13.7.0
numpy>=1.24.3
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.36.0

# Forced alignment (Montreal Forced Aligner will be installed separately via conda if needed, but WhisperX handles its own)
# montreal-forced-aligner>=2.2.0

# Subtitle generation (WhisperX uses its own internal deps + torch/audio)

# Bilibili Upload
bilibili-api-python>=17.4.1
curl_cffi>=0.5.10
